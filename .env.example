DATABASE_URL="postgresql://user:password@localhost:5432/signal_face"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN="30d"
PORT=3001

# SMTP config for sending OTP emails (verification, password reset)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="user@example.com"
SMTP_PASS="change-me"
SMTP_FROM="Signal Face <no-reply@signalface.app>"

# Firebase Admin credentials for verifying Google sign-in ID tokens.
# From Console > Project settings > Service accounts > Generate new private key.
# Keep the private key on one line with literal \n escapes, wrapped in double quotes.
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMII...\n-----END PRIVATE KEY-----\n"

# Cloudinary config for avatar/media uploads
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# Transactional email. Preferred over the SMTP_* block above: hosts commonly block
# outbound SMTP ports (Render's free instances block 25/465/587), and HTTPS is not
# blocked. When RESEND_API_KEY is set it takes precedence; otherwise SMTP is used.
RESEND_API_KEY="re_your-api-key"
# Must be an address on a domain verified in Resend, not a Gmail address.
MAIL_FROM="Signal Face <no-reply@yourdomain.com>"
